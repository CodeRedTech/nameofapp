<!DOCTYPE html>
<html>
  <head>
    <title>NameofApp</title>

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>

    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div id="wrapper">
      <nav class="navbar navbar-default">
        <div class="container-fluid">
          <!-- Brand and toggle get grouped for better mobile display -->
          <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
            <%= link_to "Red", root_path, class: "navbar-brand" %>
          </div>

          <!-- Collect the nav links, forms, and other content for toggling -->
          <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <form class="navbar-form navbar-left">
              <div class="form-group">
                <%= form_with(url: products_path, method: 'get', local: true) do |form| %>
                  <%= form.text_field :q, id: 'q', value: params[:q], class: "form-control" %>
                  <%= form.submit 'Search', class:"btn btn-default" %>
                <% end %>

                <%# form_with(url: products_path, method: 'get', local: true) do |form| %>
                  <%# collection_select(:product, :product_id, Product.all, :id, :name, class: "form-control") %>
                  <%# form.submit 'Search', class:"btn btn-default" %>
                <%# end %>
                <!-- This is for the dropdown showing all products -->
              </div>
            </form>
            <ul class="nav navbar-nav navbar-right">
              <li><%= link_to "Contact",  simple_pages_contact_path %></li>
              <li><%= link_to "About", simple_pages_about_path %></li>
              <li><%= link_to "Products", products_path %></li>
              <li>
                <% if user_signed_in? %>
                  <%= link_to('Logout', destroy_user_session_path, method: :delete) %>
                <% else %>
                  <%= link_to('Login', new_user_session_path) %>
                <% end %>
              </li>
            </ul>
          </div><!-- /.navbar-collapse -->
        </div><!-- /.container-fluid -->
      </nav>

      <% if flash[:notice] %>
        <p id="notice" class="alert alert-info"><%= flash[:notice] %></p>
      <% end %>
      <% if flash[:success] %>
        <p id="notice" class="alert alert-success"><%= flash[:success] %></p>
      <% end %>
      <% if flash[:error] %>
        <p id="notice" class="alert alert-warning"><%= flash[:error] %></p>
      <% end %>

      <div class="container">
        <%= yield %>
      </div>

      <div class="clearfix"></div>

    </div>
    <footer>
      <div class="container">
        <p class="copyright">&copy; 2018 Code Red Tech</p>
      </div>
    </footer>

  </body>
</html>
